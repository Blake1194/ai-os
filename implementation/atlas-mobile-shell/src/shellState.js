const shellState = {
  baseline: 'Usable V1 remains the stable repo-safe baseline while controlled expansion is open.',
  openLanes: [
    'installs and toolchain setup',
    'external integrations',
    'second-generation architecture',
    'Android/mobile app work'
  ],
  nextUnlock: 'Bootstrap the actual install-capable toolchain for this workspace and validate the first local run.'
};

export default shellState;
