# Resource Leverage Commands

## Use Current Do Lane Only
- "Use the current Do lane only and keep all work on the main implementation path unless a second lane is clearly necessary."

## Evaluate Whether a Second Do Lane Is Justified
- "Evaluate whether a second Do lane is actually justified, and approve it only if it reduces confusion instead of increasing it."

## Use Google Sheets Only for Structured Tracking
- "Use Google Sheets only if this work needs structured row/column tracking that markdown in the repo cannot handle cleanly."

## Avoid Opening Unnecessary Parallel Systems
- "Review the currently active tools and tell me which ones are actually needed right now versus which ones would create unnecessary parallel-system sprawl."

## Promote a Tool from Optional to Core
- "Evaluate whether this optional tool should be promoted to core for Atlas OS, and only approve it if it creates more leverage than coordination cost."

## Defer a Tool Until the Main Path Is Stable
- "Explain why this tool should be deferred until the main implementation path is stable, and state the condition that would justify using it later."
